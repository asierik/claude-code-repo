{
  "name": "mealmate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Collaborative meal planning + grocery list PWA",
  "scripts": {
    "build": "cd web && npm ci --include=dev && npx ng build",
    "start": "node server.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "dotenv": "^17.4.2",
    "express": "^4.22.2"
  },
  "engines": {
    "node": "24.15.0"
  }
}
